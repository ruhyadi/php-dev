# NOS Clone

## Command Line

Create a new model:

```bash
php artisan make:model ModelName -m
```

Create a new seeder:

```bash
php artisan make:seeder ModelNameSeeder
```

Migrate the database:

```bash
php artisan migrate
```

Seed the database:

```bash
php artisan db:seed
```